{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "NODE_ENV",
    "PORT",
    "DATABASE_URL",
    "JWT_SECRET",
    "JWT_EXPIRES_IN",
    "COOKIE_SECURE",
    "CORS_ORIGIN"
  ],
  "tasks": {
    "db:generate": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build", "db:generate"],
      "inputs": ["src/**", "tsconfig*.json", "vite.config.*"],
      "outputs": ["dist/**", ".next/**"]
    },
    "test": {
      "dependsOn": ["build"],
      "inputs": ["src/**", "test/**", "tests/**", "jest.config.*", "vite.config.*"]
    },
    "test:e2e": {
      "dependsOn": ["build"],
      "inputs": ["src/**", "test/**", "tests/**"]
    },
    "lint": {
      "dependsOn": ["^build", "db:generate"],
      "inputs": ["src/**", ".eslintrc*", "eslint.config.*"]
    },
    "format": {},
    "format:fix": {
      "cache": false
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "tsconfig*.json"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
